# Python code with simple errors

def calculate_average(numbers):
    total = 0
    for num in numbers
        total += num
    average = total / len(numbers)
    return averge  # Typo in variable name

nums = [10, 20, 30, 40, 50
print("The average is:", calculate_average(nums))

# Errors:
# 1. Missing colon (:) after for loop declaration
# 2. Typo in return statement variable name ('averge' instead of 'average')
# 3. Missing closing bracket in list definition
# 4. Missing closing parenthesis in print function
